/* Paste your deployed Vercel endpoint URL here after `vercel deploy`, e.g.
   window.CRR_FEEDBACK = { endpoint: "https://crr-radar-feedback.vercel.app/api/feedback" };
   Leaving it blank disables the feedback widget's network calls. */
window.CRR_FEEDBACK = { endpoint: "" };
